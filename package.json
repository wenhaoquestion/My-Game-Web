{
  "name": "wenhao-web-arcade",
  "version": "2.0.0",
  "private": true,
  "description": "Twelve browser games and tools, with no build step.",
  "scripts": {
    "dev": "python3 -m http.server 8000 --bind 127.0.0.1",
    "check": "node scripts/check.mjs"
  },
  "engines": { "node": ">=18" }
}
